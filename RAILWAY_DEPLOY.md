# Railway Deployment Guide

Быстрый гайд по деплою OpenClaw на Railway.

## Предварительные требования

✅ Railway account ([railway.app](https://railway.app))
✅ Railway CLI: `npm i -g @railway/cli`
✅ Git repository подключен к Railway
✅ Переменные окружения настроены

## Быстрый деплой

### Вариант 1: Через Railway Helper (рекомендуется)

```bash
# Показать статус
./scripts/railway-helper.sh status

# Задеплоить
./scripts/railway-helper.sh deploy

# Посмотреть логи
./scripts/railway-helper.sh logs

# Следить за логами
./scripts/railway-helper.sh logs-follow
```

### Вариант 2: Напрямую через Git

```bash
# Закоммитить изменения
git add .
git commit -m "your changes"

# Пушнуть на Railway (auto-deploy)
git push origin main
```

## Railway Helper Commands

```bash
./scripts/railway-helper.sh <command>
```

| Command       | Описание                             |
| ------------- | ------------------------------------ |
| `status`      | Показать статус deployment           |
| `logs`        | Последние 100 строк логов            |
| `logs-follow` | Следить за логами в реальном времени |
| `ssh`         | Открыть SSH сессию                   |
| `env`         | Показать переменные окружения        |
| `deploy`      | Задеплоить (git push)                |
| `restart`     | Перезапустить сервис                 |
| `health`      | Проверить health gateway             |

**Примечание:** Все команды используют явные флаги для идентификации проекта, окружения и сервиса. Никогда не используйте `railway login` или `railway link` напрямую - это может отвязать проект от Railway Helper.

## Переменные окружения (Railway)

Обязательные:

```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Admin IDs (owner access)
ADMIN_TELEGRAM_IDS=YOUR_TELEGRAM_ID,ANOTHER_ID

# AI Models (хотя бы один)
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# Gateway (опционально, Railway генерирует автоматически)
OPENCLAW_GATEWAY_TOKEN=<auto-generated-or-custom>
RAILWAY_PUBLIC_DOMAIN=<auto-assigned>.railway.app
```

Опциональные:

```bash
# Web Search
BRAVE_API_KEY=BSA...
FIRECRAWL_API_KEY=fc-...

# Другие модели
GEMINI_API_KEY=...
OPENROUTER_API_KEY=...
```

## Настройка Persistent Volume

Railway → Settings → Volumes → Add Volume:

```
Mount Path: /data
```

Это сохранит:

- `/data/.openclaw/users.json` (база пользователей)
- `/data/workspace` (рабочее пространство агента)
- Логи и сессии

## Проверка деплоя

### 1. Проверить статус

```bash
./scripts/railway-helper.sh status
```

### 2. Посмотреть логи

```bash
./scripts/railway-helper.sh logs
```

Должно показать:

```
✅ Gateway started on port 8080
✅ Telegram bot connected
✅ User store initialized
✅ Owners initialized: 2 users
```

### 3. Проверить Telegram бота

Напишите боту в Telegram:

```
/start
```

Должен ответить:

```
✅ Добро пожаловать!

Вам доступен бесплатный trial на 7 дней:
📨 5 сообщений в день
⏱ До 23.02.2026

Попробуйте: напишите мне любой вопрос!
```

### 4. Проверить owner доступ

Если вы в `ADMIN_TELEGRAM_IDS`:

```
/plan
```

Должен показать:

```
📊 Ваш план: Owner

⏱ Срок действия: бессрочно
📨 Лимит: безлимит
🤖 Модель: лучшая доступная
```

## Troubleshooting

### Бот не отвечает

1. Проверить логи:

   ```bash
   ./scripts/railway-helper.sh logs-follow
   ```

2. Проверить переменные:

   ```bash
   ./scripts/railway-helper.sh env
   ```

3. Перезапустить:
   ```bash
   ./scripts/railway-helper.sh restart
   ```

### "User store not initialized"

SSH в Railway и проверить:

```bash
./scripts/railway-helper.sh ssh

# Внутри SSH:
ls -la /data/.openclaw/
cat /data/.openclaw/users.json
```

### База пользователей потерялась

Проверить persistent volume:

```bash
./scripts/railway-helper.sh ssh

# Проверить volume
df -h | grep /data
ls -la /data/.openclaw/
```

Если `/data` не примонтирован - добавить volume в Railway settings.

## Мониторинг

### Логи в реальном времени

```bash
./scripts/railway-helper.sh logs-follow
```

### Health check

```bash
./scripts/railway-helper.sh health
```

### SSH для debugging

```bash
./scripts/railway-helper.sh ssh
```

Внутри SSH:

```bash
# Проверить процессы
ps aux | grep node

# Проверить порты
ss -tlnp

# Проверить логи
tail -f /app/logs/*.log

# Проверить users.json
cat /data/.openclaw/users.json | jq .
```

## Railway IDs

Проект уже настроен с этими ID:

```bash
PROJECT_ID=6a9e5acb-0d8a-4687-bef8-12148d9f3981
ENV_ID=f53aeb88-93b1-4e9b-91d9-8608e7ce2261
SERVICE_ID=848d7299-671a-4914-8cbf-3f9449047e79
```

Можно переопределить через environment variables:

```bash
export RAILWAY_PROJECT_ID=your-project-id
export RAILWAY_ENV_ID=your-env-id
export RAILWAY_SERVICE_ID=your-service-id
```

**Важно:** Railway Helper использует **явные флаги** (`--project`, `--environment`, `--service`) вместо `railway link` или `railway login`. Это предотвращает случайную отвязку проекта.

## Useful Links

- Railway Dashboard: https://railway.app/
- Docs: https://docs.openclaw.ai/
- Runbook: [RAILWAY_SESSION_RUNBOOK.md](./RAILWAY_SESSION_RUNBOOK.md)
