# Roadmap

Стратегия развития проекта: обёртки поверх ядра OpenClaw Platform.

## Принцип

Мы строим **продукт на базе OpenClaw**, а не заменяем его. Каждая фича — это обёртка вокруг стандартных механизмов ядра.

```
ЯДРО (OpenClaw Platform)     →  НЕ ТРОГАЕМ, обновляем из upstream
ОБЁРТКИ (наши фичи)          →  СТРОИМ поверх ядра
```

## Фаза 1: MVP (текущая)

**Цель**: рабочий бот с trial + подпиской

| Задача                                         | Статус | Тип     | Использует из ядра               |
| ---------------------------------------------- | ------ | ------- | -------------------------------- |
| User Store (users.json)                        | ✅     | Обёртка | Путь хранения `/data/.openclaw/` |
| Роли (owner/vip/subscriber/trial/expired)      | ✅     | Обёртка | —                                |
| Access Control (проверка перед agent dispatch) | ✅     | Обёртка | grammY middleware pipeline       |
| Команды /start, /plan, /subscribe              | ✅     | Обёртка | `customCommands` из ядра         |
| Owner-инициализация из ADMIN_TELEGRAM_IDS      | ✅     | Обёртка | ENV переменные                   |
| Daily message counter (сброс по UTC)           | ✅     | Обёртка | —                                |
| Деплой на Railway                              | ✅     | Деплой  | Dockerfile.railway               |
| Workspace templates (AGENTS.md и др.)          | ✅     | Ядро    | Agent bootstrap из templates     |

## Фаза 2: Монетизация

**Цель**: работающая оплата через Telegram Stars

| Задача                                              | Статус | Тип     | Использует из ядра                     |
| --------------------------------------------------- | ------ | ------- | -------------------------------------- |
| Payment Handlers (pre_checkout, successful_payment) | 🔲     | Обёртка | grammY `bot.on("pre_checkout_query")`  |
| sendInvoice с currency: "XTR"                       | 🔲     | Обёртка | grammY `bot.api.sendInvoice()`         |
| Auto-renewal через Telegram                         | 🔲     | Обёртка | subscription_period в sendInvoice      |
| Refund через refundStarPayment                      | 🔲     | Обёртка | grammY Bot API                         |
| Plan upgrade (starter → premium)                    | 🔲     | Обёртка | User Store                             |
| Подписочные уведомления (истекает через N дней)     | 🔲     | Обёртка | Cron из ядра `cron: { enabled: true }` |

**Ключевое**: вся платёжная логика через стандартный grammY из ядра, мы только добавляем бизнес-логику (какой план, сколько стоит, когда продлять).

## Фаза 3: Рост

**Цель**: привлечение пользователей

| Задача                                | Статус | Тип     | Использует из ядра          |
| ------------------------------------- | ------ | ------- | --------------------------- |
| VIP Invite-ссылки (deep linking)      | 🔲     | Обёртка | grammY deep linking         |
| Реферальная система                   | 🔲     | Обёртка | User Store                  |
| /admin команды (grant, revoke, stats) | 🔲     | Обёртка | `customCommands`            |
| Аналитика использования для owner     | 🔲     | Обёртка | User Store + usage tracking |
| Миграция User Store на SQLite         | 🔲     | Обёртка | —                           |

## Фаза 4: Продвинутые фичи

**Цель**: использование полного потенциала ядра OpenClaw

| Задача                         | Статус | Тип     | Использует из ядра               |
| ------------------------------ | ------ | ------- | -------------------------------- |
| Model routing по тарифу        | 🔲     | Обёртка | `agents.defaults.model` config   |
| Tool restrictions по роли      | 🔲     | Обёртка | `tools: { allow, deny }` из ядра |
| Skills для подписчиков         | 🔲     | Обёртка | Skills system из ядра            |
| Webhook mode (вместо polling)  | 🔲     | Конфиг  | `channels.telegram.webhookUrl`   |
| Group support                  | 🔲     | Конфиг  | `channels.telegram.groups`       |
| Browser tool для premium       | 🔲     | Конфиг  | `browser: { enabled: true }`     |
| Multi-agent (private + public) | 🔲     | Конфиг  | `agents.list` из ядра            |
| Hooks для event tracking       | 🔲     | Обёртка | Hooks system из ядра             |

## Что НЕ делаем

Эти вещи уже есть в ядре OpenClaw — НЕ создаём свои замены:

- ❌ Свой config формат (config.yaml) → используем `openclaw.json`
- ❌ Свою session management → используем OpenClaw sessions
- ❌ Свой Telegram клиент → используем grammY из ядра
- ❌ Свой model routing → используем `agents.defaults.model`
- ❌ Свои tools/skills → используем Tools & Skills из ядра
- ❌ Свой gateway → используем OpenClaw Gateway
- ❌ Свой agent runtime → используем Pi embedded runner

## Как добавлять новые фичи

1. **Проверь** — есть ли это в ядре OpenClaw? → [docs.openclaw.ai](https://docs.openclaw.ai)
2. **Если есть** — используй через конфигурацию `openclaw.json`
3. **Если нет** — создай обёртку в `src/telegram/` (НЕ модифицируй core файлы ядра)
4. **Документируй** — укажи в docs что это обёртка и что из ядра используется
5. **Проверь Context7** — актуальная документация через `mcp__context7__query-docs`
