# VIP Invite-ссылки

Как получить и использовать VIP invite-ссылки для бесплатного полного доступа.

## Что такое VIP invite?

**VIP invite-ссылка** — это персональное приглашение от владельца бота, которое дает **бесплатный полный доступ** без ограничений и подписки.

### Преимущества VIP

| Параметр          | VIP       | Premium (платный)    |
| ----------------- | --------- | -------------------- |
| **Стоимость**     | Бесплатно | $3.90/мес            |
| **Срок действия** | Бессрочно | 30 дней + auto-renew |
| **Лимиты**        | Безлимит  | Безлимит             |
| **Модель**        | Лучшая    | Лучшая               |
| **Инструменты**   | Все       | Все                  |

VIP = **бесплатный Premium навсегда** 🎁

## Как получить VIP invite?

VIP invite-ссылки выдаются **только владельцем бота** (owner).

### Вариант 1: Попросить у владельца

1. Свяжитесь с владельцем бота (например, через личные сообщения)
2. Попросите VIP invite
3. Владелец создаст ссылку через `/invite [label]`
4. Получите ссылку вида: `https://t.me/openclaw_jicool_bot?start=inv_abc123def456`

### Вариант 2: Реферал от друга (если включено)

Если владелец включил реферальную систему, вы можете получить VIP доступ пригласив друзей.

Подробнее: [Реферальная система](/users/referrals)

## Как использовать invite-ссылку?

### Если вы НОВЫЙ пользователь

1. Перейдите по ссылке: `https://t.me/openclaw_jicool_bot?start=inv_abc123`
2. Telegram откроет бота
3. Нажмите **Start**
4. Бот автоматически активирует VIP доступ

```
✅ VIP доступ активирован!

Вы получили полный доступ по приглашению.

📨 Лимит: безлимит
🤖 Модель: лучшая доступная
⏱ Срок: бессрочно

Спасибо за присоединение!
```

### Если вы УЖЕ ПИСАЛИ боту раньше

⚠️ **Важно**: Payload (`inv_abc123`) работает **только при первом `/start`**.

Если вы уже ранее писали боту, используйте команду `/redeem`:

```
/redeem inv_abc123def456
```

Бот активирует VIP доступ вручную:

```
✅ VIP доступ активирован!

Invite-код принят.
Ваша роль обновлена: Trial → VIP

📨 Лимит: безлимит
🤖 Модель: лучшая доступная
⏱ Срок: бессрочно
```

## Формат invite-ссылки

### Deep Link

```
https://t.me/openclaw_jicool_bot?start=inv_<код>
```

- **Telegram bot username**: `openclaw_jicool_bot`
- **Payload**: `inv_<код>` (префикс `inv_` обязателен)

### Payload структура

Пример: `inv_abc123def456`

- `inv_` — префикс (означает "invite")
- `abc123def456` — base64url(userId + timestamp + random)

Код:

- До 64 символов
- Только `A-Z a-z 0-9 _ -`
- Уникальный для каждого invite

## Для владельцев: как создать invite

### Команда `/invite`

```
/invite [label]
```

**Label** — метка для вашего удобства (например, имя человека).

**Пример**:

```
/invite mama
```

**Ответ бота**:

```
✅ VIP invite-ссылка создана:

https://t.me/openclaw_jicool_bot?start=inv_abc123def456

Label: mama
Срок действия: бессрочно
Тип доступа: VIP (полный доступ без оплаты)

Отправьте эту ссылку вашему другу.
```

### Что происходит внутри?

1. Бот генерирует уникальный код:

   ```typescript
   const payload = `inv_${base64url(userId + timestamp + random)}`;
   ```

2. Сохраняет в базу:

   ```json
   {
     "code": "inv_abc123def456",
     "label": "mama",
     "createdBy": 123456789, // ваш owner ID
     "createdAt": 1708128000,
     "used": false,
     "usedBy": null
   }
   ```

3. Возвращает ссылку

### Проверка статуса invite

```
/admin invites
```

Список всех созданных invite-ссылок:

```
📋 VIP Invite-ссылки (всего: 3)

1. mama (inv_abc123def456)
   Создан: 16.02.2026
   Статус: использован @username (ID: 987654321)

2. friend_anna (inv_xyz789abc012)
   Создан: 15.02.2026
   Статус: не использован

3. colleague (inv_def456ghi789)
   Создан: 14.02.2026
   Статус: использован @another (ID: 111222333)
```

## Безопасность

### Можно ли использовать один код несколько раз?

**Зависит от настроек владельца**:

- **One-time use** (по умолчанию): код работает **только 1 раз**
  - После активации код помечается как `used`
  - Повторное использование → ошибка

- **Multi-use** (если включено): код можно использовать **несколько раз**
  - Владелец может создавать многоразовые коды
  - Полезно для групп друзей/семьи

### Можно ли отозвать invite?

Да, владелец может отозвать invite:

```
/admin revoke-invite inv_abc123def456
```

После отзыва:

- Код становится недействительным
- Новые пользователи не могут его использовать
- **Уже активированные VIP** остаются VIP (если owner не отберет права вручную)

### Можно ли заблокировать VIP пользователя?

Да, владелец может забрать VIP доступ:

```
/admin revoke 987654321
```

Пользователь переходит в статус `expired` (2 сообщения/день).

## FAQ

### Я получил invite, но уже писал боту раньше

Используйте `/redeem` вместо перехода по ссылке:

```
/redeem inv_abc123def456
```

### Код говорит "недействительный"

Возможные причины:

1. **Код уже использован** (если one-time use)
2. **Код отозван** владельцем
3. **Опечатка** в коде

Попросите владельца создать новый код.

### Можно ли передать invite другу?

Да, invite-ссылки можно свободно передавать. Но учтите:

- Если код **one-time**, первый использовавший получит VIP
- Если код **multi-use**, несколько человек могут использовать его

### VIP доступ может закончиться?

Нет, VIP доступ **бессрочный**. Единственный способ потерять его:

- Владелец отберет права через `/admin revoke`
- Бот будет удален/остановлен

### Что если владелец изменит тарифы?

VIP пользователи **не затрагиваются**. У них всегда полный доступ независимо от изменений в платных тарифах.

## Примеры использования

### Сценарий 1: Приглашение мамы

**Владелец**:

```
/invite mama
```

Получает ссылку: `https://t.me/openclaw_jicool_bot?start=inv_abc123`

Отправляет маме в личку.

**Мама**:

- Переходит по ссылке
- Нажимает Start
- ✅ VIP доступ активирован

---

### Сценарий 2: Друг уже писал боту

**Друг** раньше пробовал бота (trial).

**Владелец**:

```
/invite friend_john
```

Получает код: `inv_xyz789`

Отправляет другу **только код** (не ссылку).

**Друг**:

```
/redeem inv_xyz789
```

✅ Trial → VIP

---

### Сценарий 3: Многоразовый код для семьи

**Владелец** хочет дать доступ всей семье одним кодом:

```
/invite family --multi-use
```

Получает: `inv_family123`

Отправляет в семейный чат.

**Все члены семьи** могут использовать этот код:

- Мама: `https://t.me/bot?start=inv_family123` → VIP
- Папа: `/redeem inv_family123` → VIP
- Сестра: `https://t.me/bot?start=inv_family123` → VIP

## Что дальше?

- [Тарифные планы](/payment/plans) — сравнение VIP с другими тарифами
- [Реферальная система](/users/referrals) — как получить VIP пригласив друзей
- [Команды бота](/users/commands) — полный список команд
